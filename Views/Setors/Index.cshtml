@model IEnumerable<TaskList.Models.Setor>

@{
    ViewData["Title"] = "Index";
}

<h1>Cadastro de Setores</h1>

<p>
    <a asp-action="Create"><img src="~/adicionar.png" alt="adcionar" style="width:20px; height:20px;" /> Novo Cadastro</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeSetor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeSetor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSetor"><img src="~/Edit.png" alt="Detalhes" style="width:20px; height:20px;" /></a> |
                <a asp-action="Details" asp-route-id="@item.IdSetor"><img src="~/Details.png" alt="Detalhes" style="width:20px; height:20px;" /></a> |
                <a asp-action="Delete" asp-route-id="@item.IdSetor"><img src="~/Delete.png" alt="Detalhes" style="width:20px; height:20px;" /></a>
            </td>
        </tr>
}
    </tbody>
</table>
